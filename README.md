# embedded-source-schema
embedded source schema is a universal XMP-based Standard for Embedding Generative Source Payloads in Digital Assets. Example use case: embed the markdown source of a diagram in the image rendered based on it, using engines like kroki
